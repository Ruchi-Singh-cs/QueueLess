{
  "name": "queueless-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.js",
    "start": "node --env-file=.env src/index.js",
    "test": "node --test",
    "seed": "node --env-file=.env seed.js"
  },
  "dependencies": {
    "bcryptjs": "^3",
    "express": "^5",
    "jsonwebtoken": "^9",
    "mongoose": "^8",
    "socket.io": "^4"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10"
  },
  "allowScripts": {
    "mongodb-memory-server@10.4.3": true
  }
}
